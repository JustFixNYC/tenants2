name: Boop

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/Checkout@v2
      - name: Booooop
        run: echo "BOOP"
