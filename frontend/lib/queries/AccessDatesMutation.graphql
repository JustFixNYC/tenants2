mutation AccessDatesMutation($input: AccessDatesInput!) {
    output: accessDates(input: $input) {
        errors {
            field,
            messages
        },
        session {
            accessDates
        }
    }
}
