query DataDrivenOnboardingSuggestions($address: String!, $borough: String!) {
  output: ddoSuggestions(address: $address, borough: $borough) {
    fullAddress,
    suggestions {
      name,
      url,
    }
  }
}
