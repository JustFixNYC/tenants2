mutation ExampleMutation($input: ExampleInput!) {
    output: example(input: $input) {
        errors {
            field,
            extendedMessages {
                message,
                code
            }
        },
        response
    }
}
