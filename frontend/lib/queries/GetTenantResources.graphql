query GetTenantResources($latitude: Float!, $longitude: Float!) {
    tenantResources(latitude: $latitude, longitude: $longitude) {
        name,
        website,
        address,
        latitude,
        longitude,
        milesAway
    }
}
