mutation IssueAreaMutation($input: IssueAreaInput!) {
  output: issueArea(input: $input) {
    errors {
      field,
      messages
    }
    session {
      issues
      customIssues {
          area
          description
      }
    }
  }
}
