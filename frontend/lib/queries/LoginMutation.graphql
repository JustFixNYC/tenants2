mutation LoginMutation($input: LoginInput!) {
    output: login(input: $input) {
        errors {
            field,
            messages
        },
        session {
            ...AllSessionInfo
        }
    }
}
