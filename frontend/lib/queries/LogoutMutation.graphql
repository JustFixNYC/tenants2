mutation LogoutMutation($input: LogoutInput!) {
    output: logout(input: $input) {
        errors {
            field,
            messages
        }
        session {
            ...AllSessionInfo
        }
    }
}
